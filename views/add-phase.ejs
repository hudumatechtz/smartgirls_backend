<%- include("./includes/admin-header") -%>
<!-- CONTENTS -->
<div class="add-article">
  <div class="container">
    <div class="add-article-form">
      <form name="form" method="POST" action="/phases-admin/<%= activity._id %>/add-phase" enctype="multipart/form-data">
        <legend style="text-align: center; font-size: 30px;">ADD NEW PHASE in <%= activity.year %> </legend>
        <div class="form-group">
          <label for="title">Phase number</label>
          <input type="number" class="form-control" id="title" placeholder="Phase number" name="phaseNumber">
        </div>

        <div class="form-group">
          <label for="theme">Phase Theme</label>
          <input type="text" class="form-control" id="description" placeholder="phase theme" name="theme">
        </div>
        <div class="form-group">
          <label for="description">Description</label>
          <textarea name="description" id="ta" rows="10" placeholder="description" style="width: 100%;"></textarea>
        </div>


        <% if(message != ''){ %>
        <div style="width: 100%; text-align: center;">
          <h4 style="color: brown;">
            <%= message %>
          </h4>
        </div>
        <% } %>
        <div class="text-center">
          <button type="submit" class="btn btn-primary" id="login-button">Save phase</button>
        </div>
        <!-- <div class="not-spin">
        <div class="spinner-border" role="status">
            <span class="sr-only">Loading...</span>
        </div>
    </div> -->
      </form>
    </div>
  </div>
</div>


<%- include("./includes/admin-footer") -%>